# Client default for the Demo Mode toggle. Set to false to start in live mode.
NEXT_PUBLIC_DEFAULT_DEMO_MODE=true

# Optional path to the Circle CLI binary if `circle` is not on PATH.
# CIRCLE_CLI_PATH=/usr/local/bin/circle
